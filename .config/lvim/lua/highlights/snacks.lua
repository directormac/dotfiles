return function(colors)
    return {
        SnacksInputBorder = { bg = colors.bg1, fg = colors.bg1 },
        SnacksInputTitle = { bg = colors.blue, fg = colors.bg1 },
        SnacksInputNormal = { bg = colors.bg1, fg = colors.bg3 },
    }
end
