#!/usr/bin/env bash

RASI="$HOME/.config/rofi/emoji.rasi"

rofi_focused="$HOME/.config/hypr/scripts/rofi_focused"

# Run
"$rofi_focused" \
  -modi emoji \
  -show emoji \
  -kb-secondary-copy "" \
  -kb-custom-1 Ctrl+c \
  -theme ${RASI}
